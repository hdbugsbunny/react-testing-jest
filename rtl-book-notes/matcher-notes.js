[{"content":"# Matchers\n\nMatchers help make sure that a value is what we expect it to be.  \n\nA project generated by Create React App has access to all the matchers included in Jest, as well as matchers defined in the @testing-library/jest-dom package.\n\n| Name                      | Link                                                                                       |\n|---------------------------|--------------------------------------------------------------------------------------------|\n| Jest                      | https://jestjs.io/docs/mock-function-api\n| @testing-library/jest-dom | https://github.com/testing-library/jest-dom |","type":"text","id":"4lzss"},{"content":"import { render, screen, within } from '@testing-library/react';\n\nconst FormData = () => {\n  return (\n    <div>\n      <button>Go Back</button>\n      <form aria-label=\"form\">\n        <button>Save</button>\n        <button>Cancel</button>\n      </form>\n    </div>\n  );\n};\n\nrender(<FormData />);","type":"code","id":"uw5y8"},{"content":"// Best Option to Make Custom Matchers\nconst toContainRole = (container, role, quantity = 1) => {\n  const elements = within(container).queryAllByRole(role);\n\n  if (elements.length === quantity) {\n    return { pass: true };\n  }\n\n  return {\n    pass: false,\n    message: () =>\n      `Expected to find ${quantity} ${role} elements. Found ${elements.length} instead`,\n  };\n};\n\nexpect.extend({ toContainRole });","type":"code","id":"lyvyz"},{"content":"test('the forms displays two buttons', () => {\n  render(<FormData />);\n\n  const form = screen.getByRole('form');\n  // Using within is an option for it\n  // const buttons = within(form).getAllByRole('button');\n\n  // getAllBy -- fails as it searches all buttons\n  // const buttons = screen.getAllByRole('button');\n\n  // Custom Matcher\n  expect(form).toContainRole('button', 2);\n});","type":"code","id":"6mwaw"}]